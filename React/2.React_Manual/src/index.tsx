import * as React from 'react';
import * as ReactDOM from 'react-dom/client';

const num:number = 10;
const test = <div>끝입니다.</div>

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(test);